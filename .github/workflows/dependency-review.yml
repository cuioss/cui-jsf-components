# Example: Copy this to your repo as .github/workflows/dependency-review.yml
name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  dependency-review:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependency-review.yml@9cffe3a2febb07d668784a1257b025101228754d # v0.27.0
    permissions:
      contents: read
      pull-requests: write
