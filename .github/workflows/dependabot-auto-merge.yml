name: Dependabot Auto-merge

on:
  pull_request:

permissions:
  contents: read

jobs:
  auto-merge:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependabot-auto-merge.yml@455b5eb6471b4a2df3c708fae5a0437a113c5ad4 # v0.15.5
    permissions:
      contents: write
      pull-requests: write
