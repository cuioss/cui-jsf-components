name: Dependabot Auto-merge

on:
  pull_request:

permissions:
  contents: read

jobs:
  auto-merge:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependabot-auto-merge.yml@a2ee815b81b2ea8eb6262c60877754b998ef065f # v0.6.3
    permissions:
      contents: write
      pull-requests: write
