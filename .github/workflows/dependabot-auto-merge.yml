name: Dependabot Auto-merge

on:
  pull_request:

permissions:
  contents: read

jobs:
  auto-merge:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependabot-auto-merge.yml@baf31841ab596ad200a8360d2f969b1f4bf889ef # v0.16.1
    permissions:
      contents: write
      pull-requests: write
