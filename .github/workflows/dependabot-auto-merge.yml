name: Dependabot Auto-merge

on:
  pull_request:

permissions:
  contents: read

jobs:
  auto-merge:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependabot-auto-merge.yml@4962f9875826f8f73cd04987278f30b6cd8365d7 # v0.21.0
    permissions:
      contents: write
      pull-requests: write
